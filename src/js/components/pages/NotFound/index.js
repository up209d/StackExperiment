import React from 'react';
import styled from 'styled-components';

const StyledNotFound = styled.div`
  
`;

const NotFound = () => (
  <StyledNotFound class={'dl-not-found'}>
    <h1>NOT FOUND</h1>
  </StyledNotFound>
);

export default NotFound;
