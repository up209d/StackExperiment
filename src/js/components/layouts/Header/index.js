import React from 'react';
import styled from 'styled-components';

const StyledHeader = styled.header`
  
`;

const Header = () => (
  <StyledHeader class={'dl-header'}>
    <h1>HEADER</h1>
  </StyledHeader>
);

export default Header;
