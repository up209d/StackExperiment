import React from 'react';
import styled from 'styled-components';

const StyledHome = styled.div`
  
`;

const Home = () => (
  <StyledHome class={'dl-home'}>
    <h1>HOME</h1>
  </StyledHome>
);

export default Home;
