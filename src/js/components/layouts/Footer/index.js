import React from 'react';
import styled from 'styled-components';

const StyledFooter = styled.footer`
  
`;

const Footer = () => (
  <StyledFooter class={'dl-footer'}>
    <h1>FOOTER</h1>
  </StyledFooter>
);

export default Footer;
